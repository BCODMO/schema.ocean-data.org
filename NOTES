RDF Converters:
http://rdf-translator.appspot.com/
- XML to JSON-LD, N-Triples

README.html file is made by:

1) sending README.md to https://cloudconvert.com/md-to-html
2) converting all 'src="html/voc/static/schema/diagrams/' to 'src="/voc/static/schema/diagrams/'
3) removing '<p><a id="top"></a></p>' from the
4) copying contents to documentation.html in b/w
  	<!-- START OF Markdown -->
  	<!-- END OF Markdown -->


Build Golang:
CGO_ENABLED=0 env GOOS=linux go build .

Build Docker:
docker build -t "bcodmo/schemaodo:latest" -t "bcodmo/schemaodo:(semver)" .
docker save "bcodmo/schemaodo:latest" | bzip2 | ssh <user@host> 'bunzip2 | docker load'
